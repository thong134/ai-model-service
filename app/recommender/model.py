"""Deprecated module.

Use ``app.recommender_system`` instead of ``app.recommender.model``. This stub
remains only to avoid import errors while legacy references are cleaned up.
"""

from __future__ import annotations

raise ImportError("'app.recommender.model' has been deprecated; import from 'app.recommender_system' instead.")
